name: Hello World Workflow

on:
  push:
    branches:
      - main   # ye workflow tab chalega jab "main" branch par push hoga

jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Print Hello World
        run: echo "Hello, World! 🎉"
